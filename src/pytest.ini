[pytest]

addopts = -ra -s -v --flakes --cov-report=html --cov=.

; exclude unrelated folders and all old tests
norecursedirs =
    .*
    env
    venv

flakes-ignore =
  *.py UnusedImport
  *.py UnusedVariable
